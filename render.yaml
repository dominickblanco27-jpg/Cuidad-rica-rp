services:
  - type: web
    name: discord-bot
    runtime: node
    buildCommand: npm install -g pnpm && pnpm install
    startCommand: node src/deploy-commands.js && node src/index.js
    envVars:
      - key: DISCORD_TOKEN
        sync: false
      - key: DISCORD_CLIENT_ID
        sync: false
